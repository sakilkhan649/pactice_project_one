import 'package:flutter/material.dart';

class MeatlsScreen extends StatelessWidget {
  const MeatlsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
      body: Center(
        child: Text("Meals",style: TextStyle(color: Colors.white),),
      ),
      
    );
  }
}
